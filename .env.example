# TanStack Mini Admin Dashboard Environment Variables
# Since this dashboard runs entirely client-side with mock localStorage persistence,
# no external API keys are required for basic usage.

VITE_APP_NAME="TanStack Mini Admin Dashboard"
VITE_API_URL="http://localhost:3000/api"
VITE_MOCK_DELAY_MS=800
